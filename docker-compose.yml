---
version: '2'

services:
  postgres:
    image: 'postgres:10.3-alpine'
    volumes:
      - 'postgres:/var/lib/postgresql/data'
    env_file:
      - '.env'

  redis:
    image: 'redis:4.0-alpine'
    command: redis-server
    volumes:
      - 'redis:/data'

  website:
    build: .
    depends_on:
      - 'postgres'
      - 'redis'
    ports:
      - '3000:3000'
    volumes:
      - '.:/railsapp'
    env_file:
      - '.env'

  sidekiq:
    depends_on:
      - 'postgres'
      - 'redis'
    build: .
    command: sidekiq -C config/sidekiq.yml
    volumes:
      - '.:/railsapp'
    env_file:
      - '.env'

  cable:
    depends_on:
      - 'redis'
    build: .
    command: puma -p 28080 -C config/cable.config.ru
    ports:
      - '28080:28080'
    volumes:
      - '.:/railsapp'
    env_file:
      - '.env'

volumes:
  redis:
  postgres:
